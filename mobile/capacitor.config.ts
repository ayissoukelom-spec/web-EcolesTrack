import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ecoletrack.mobile',
  appName: 'ecoletrack-mobile',
  webDir: 'dist'
};

export default config;
