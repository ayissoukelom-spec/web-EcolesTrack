diff --git a/server.ts b/server.ts
index 49423f8..4cc1d1b 100644
--- a/server.ts
+++ b/server.ts
@@ -550,4 +549,0 @@ export async function createApp() {
-      if (actorRole !== 'super_admin') {
-        return res.status(403).json({ error: 'Forbidden: only super_admin can manage multi-school memberships' });
-      }
-
@@ -723,6 +718,0 @@ export async function createApp() {
-      } else if (req.user && req.user.email) {
-        const rows = await db.select().from(users).where(eq(users.email, req.user.email));
-        dbUser = rows.length > 0 ? rows[0] : null;
-      } else if (req.user && req.user.uid) {
-        const rows = await db.select().from(users).where(eq(users.uid, req.user.uid));
-        dbUser = rows.length > 0 ? rows[0] : null;
@@ -764 +753,0 @@ export async function createApp() {
-        reqUser: req.user,
@@ -770,5 +758,0 @@ export async function createApp() {
-      // If the simulated actor exists in DB without a schoolId, fall back to header schoolId.
-      if (actor.role === 'school_admin' && actor.schoolId == null && req.user?.simulated && req.user.schoolId != null) {
-        actor = { ...actor, schoolId: req.user.schoolId } as any;
-      }
-
@@ -1791,3 +1775,3 @@ export async function createApp() {
-      // Load user from database to get school_id (with simulated fallback)
-      const user = await resolveActor(req);
-      if (!user) return res.status(401).json({ error: 'Unauthenticated' });
+      // Load actor from database to get school_id (with simulated fallback)
+      const actor = await resolveActor(req);
+      if (!actor) return res.status(401).json({ error: 'Unauthenticated' });
@@ -1796 +1780 @@ export async function createApp() {
-      if (user.role === 'super_admin') {
+      if (actor.role === 'super_admin') {
@@ -1799 +1783 @@ export async function createApp() {
-      } else if ((user.role === 'school_admin' || user.role === 'parent' || user.role === 'teacher') && user.schoolId) {
+      } else if ((actor.role === 'school_admin' || actor.role === 'parent' || actor.role === 'teacher') && actor.schoolId) {
@@ -1801 +1785 @@ export async function createApp() {
-        list = await db.select().from(schools).where(eq(schools.id, user.schoolId));
+        list = await db.select().from(schools).where(eq(schools.id, actor.schoolId));
@@ -1803 +1787 @@ export async function createApp() {
-        // Other roles cannot access schools list, or user without assigned school
+        // Other roles cannot access schools list, or actor without assigned school
@@ -1819,4 +1803,4 @@ export async function createApp() {
-      // Load user and validate permission
-      const user = await resolveActor(req);
-      if (!user) {
-        console.log('TRACE /api/evaluations about to return 404 after resolveActor', { reqUser: req.user });
+      // Load actor and validate permission
+      const actor = await resolveActor(req);
+      if (!actor) {
+        console.log('TRACE /api/evaluations about to return 404 after resolveActor', { actor: null });
@@ -1827 +1811 @@ export async function createApp() {
-      if (user.role !== 'super_admin') {
+      if (actor.role !== 'super_admin') {
@@ -1907,3 +1891,3 @@ export async function createApp() {
-      // Load user and validate school permission
-      const user = await resolveActor(req);
-      if (!user) return res.status(404).json({ error: 'User not found' });
+      // Load actor and validate school permission
+      const actor = await resolveActor(req);
+      if (!actor) return res.status(404).json({ error: 'User not found' });
@@ -1912 +1896 @@ export async function createApp() {
-      if (user.role !== 'super_admin') {
+      if (actor.role !== 'super_admin') {
@@ -2036,2 +2020,2 @@ export async function createApp() {
-      const user = await resolveActor(req);
-      if (!user) return res.status(401).json({ error: 'Unauthenticated' });
+      const actor = await resolveActor(req);
+      if (!actor) return res.status(401).json({ error: 'Unauthenticated' });
@@ -2042 +2026 @@ export async function createApp() {
-      if (user.role === 'super_admin' || (user.schoolId && Number(user.schoolId) === id)) {
+      if (actor.role === 'super_admin' || (actor.schoolId && Number(actor.schoolId) === id)) {
@@ -2223,2 +2207,2 @@ export async function createApp() {
-      const user = await resolveActor(req);
-      if (!user) {
+      const actor = await resolveActor(req);
+      if (!actor) {
@@ -2228 +2212 @@ export async function createApp() {
-      if (user.role !== 'super_admin') {
+      if (actor.role !== 'super_admin') {
@@ -2354,2 +2338,2 @@ export async function createApp() {
-        if (user.academicYearId) {
-          list = await db.select().from(academicYears).where(eq(academicYears.id, user.academicYearId));
+        if (actor.academicYearId) {
+          list = await db.select().from(academicYears).where(eq(academicYears.id, actor.academicYearId));
@@ -2365 +2349 @@ export async function createApp() {
-            eq(academicYears.schoolId, user.schoolId)
+            eq(academicYears.schoolId, actor.schoolId)
@@ -2813 +2797 @@ export async function createApp() {
-      if (user.role === 'school_admin' && resolvedSchoolId == null) {
+      if (actor.role === 'school_admin' && resolvedSchoolId == null) {
@@ -4100,2 +4084,2 @@ export async function createApp() {
-      const user = await resolveActor(req);
-      if (!user) return res.status(404).json({ error: 'User not found' });
+      const actor = await resolveActor(req);
+      if (!actor) return res.status(404).json({ error: 'User not found' });
@@ -4105,4 +4089,4 @@ export async function createApp() {
-      const targetSchoolId = user.role === 'school_admin'
-        ? user.schoolId
-        : user.role === 'teacher'
-          ? user.schoolId
+      const targetSchoolId = actor.role === 'school_admin'
+        ? actor.schoolId
+        : actor.role === 'teacher'
+          ? actor.schoolId
@@ -4111 +4095 @@ export async function createApp() {
-      if (user.role === 'teacher') {
+      if (actor.role === 'teacher') {
@@ -4154 +4138 @@ export async function createApp() {
-      if (user.role === 'school_admin') {
+      if (actor.role === 'school_admin') {
@@ -4198 +4182 @@ export async function createApp() {
-        if (user.role === 'super_admin') {
+        if (actor.role === 'super_admin') {
@@ -4409 +4393 @@ export async function createApp() {
-      if (user.role === 'school_admin' && user.schoolId !== schoolId) {
+      if (actor.role === 'school_admin' && actor.schoolId !== schoolId) {
@@ -4460 +4444 @@ export async function createApp() {
-      if (user.role === 'school_admin' && user.schoolId !== schoolId) {
+      if (actor.role === 'school_admin' && actor.schoolId !== schoolId) {
@@ -4608 +4592 @@ export async function createApp() {
-      if (user.role === 'teacher') {
+      if (actor.role === 'teacher') {
@@ -4702 +4686 @@ export async function createApp() {
-        if (user.schoolId != null) {
+        if (actor.schoolId != null) {
@@ -4706 +4690 @@ export async function createApp() {
-              eq(schoolClasses.schoolId, user.schoolId),
+              eq(schoolClasses.schoolId, actor.schoolId),
@@ -4713 +4697 @@ export async function createApp() {
-              userSchoolId: user.schoolId,
+              userSchoolId: actor.schoolId,
@@ -4718 +4702 @@ export async function createApp() {
-          approvalSchoolId = user.schoolId;
+          approvalSchoolId = actor.schoolId;
@@ -4754 +4738 @@ export async function createApp() {
-        userSchoolId: user.schoolId,
+        userSchoolId: actor.schoolId,
@@ -4837,3 +4821,3 @@ export async function createApp() {
-      if (user.role !== 'super_admin') {
-        if (user.role === 'parent') {
-          const childStudentIds = await getParentChildStudentIds(user.id);
+      if (actor.role !== 'super_admin') {
+        if (actor.role === 'parent') {
+          const childStudentIds = await getParentChildStudentIds(actor.id);
@@ -4847,2 +4831,2 @@ export async function createApp() {
-          if (user.schoolId) {
-            query = query.where(eq(students.schoolId, user.schoolId)) as any;
+          if (actor.schoolId) {
+            query = query.where(eq(students.schoolId, actor.schoolId)) as any;
@@ -4898 +4882 @@ export async function createApp() {
-        userSchoolId: user.schoolId,
+        userSchoolId: actor.schoolId,
@@ -4952 +4936 @@ export async function createApp() {
-        if (user.role === 'teacher') {
+        if (actor.role === 'teacher') {
